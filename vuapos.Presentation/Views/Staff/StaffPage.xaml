<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="vuapos.Presentation.Views.Staff.StaffPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:vuapos.Presentation.Views.Staff"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <UserControl.Resources>
        <local:UsernameToInitialsConverter x:Key="InitialsConverter"/>

        <local:UsernameToColorConverter x:Key="ColorConverter"/>
    </UserControl.Resources>

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <Button Content="Add New Staff" Width="150" Height="40" 
            Click="AddButton_Click" Background="#C19256" Foreground="White" 
            BorderBrush="Transparent" CornerRadius="5" Grid.Column="0">
                <Button.ContentTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE710;" 
                                  FontFamily="Segoe MDL2 Assets" 
                                  FontSize="12" 
                                  Margin="0,0,8,0"/>
                        <TextBlock Text="Add New Staff"/>
                    </StackPanel>
                </DataTemplate>
                </Button.ContentTemplate>
            </Button>
            
            <StackPanel Grid.Column="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <TextBlock Text="Filter by" 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
                <TextBox Width="200" 
                         PlaceholderText="Search in Members..." 
                         VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" 
              Background="#F5F5F5" 
              Margin="0,15,0,0" 
              Padding="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <CheckBox Grid.Column="0" IsChecked="False"/>
            <TextBlock Grid.Column="1" FontWeight="Bold" Text="#" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="2" FontWeight="Bold" Text="Member" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="3" FontWeight="Bold" Text="Role" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="4" FontWeight="Bold" Text="Password" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="5" FontWeight="Bold" Text="Phone Number" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="6" FontWeight="Bold" Text="Operations" VerticalAlignment="Center"/>
        </Grid>

        <!-- Staff List -->
        <ListView Grid.Row="2" 
                  x:Name="StaffListView"
                  ItemsSource="{Binding Staffs}"
                  SelectedItem="{Binding SelectedStaff, Mode=TwoWay}"  
                  SelectionMode="Single" 
                  Padding="0"
                  Background="Transparent"
                  BorderThickness="0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="MinHeight" Value="48"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Grid x:Name="ContentBorder" 
                                      Background="{TemplateBinding Background}" 
                                      BorderThickness="0,0,0,1" 
                                      BorderBrush="#EEEEEE">
                                    <ContentPresenter Content="{TemplateBinding Content}"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Column="0" 
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                  VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="1" 
                                   Text="{Binding StaffId}" 
                                   VerticalAlignment="Center"/>

                        <StackPanel Grid.Column="2" 
                                    Orientation="Horizontal" 
                                    VerticalAlignment="Center">
                            <Border Width="32" 
                                    Height="32" 
                                    CornerRadius="16" 
                                    Background="{Binding Username, Converter={StaticResource ColorConverter}}" 
                                    Margin="0,0,10,0">
                                <TextBlock Text="{Binding Username, Converter={StaticResource InitialsConverter}}" 
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Center" 
                                           Foreground="White"/>
                            </Border>
                            <TextBlock Text="{Binding Username}" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <TextBlock Grid.Column="3" 
                                   Text="{Binding Role}" 
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="4" 
                                   Text="{Binding Password}" 
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="5" 
                                   Text="{Binding Phone}" 
                                   VerticalAlignment="Center"/>

                        <StackPanel Grid.Column="6" 
                                    Orientation="Horizontal" 
                                    VerticalAlignment="Center">
                            <Button Style="{StaticResource ActionButtonStyle}"
                                    Tag="{Binding}"
                                     Click="EditButton_Click"
                                   >
                                <FontIcon Glyph="&#xE70F;" 
                                          FontFamily="Segoe MDL2 Assets" 
                                          Foreground="#5B5FC7"/>
                            </Button>
                            <Button Style="{StaticResource ActionButtonStyle}"
                                    Margin="5,0,0,0"
                                    Tag="{Binding}"
                                    Click="DeleteButton_Click"
                             >
                                <FontIcon Glyph="&#xE74D;" 
                                          FontFamily="Segoe MDL2 Assets" 
                                          Foreground="#E74C3C"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Add Staff Button -->
        
    </Grid>
</UserControl>