<UserControl
    x:Class="vuapos.Presentation.Views.Customer.CustomerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:vuapos.Presentation.Views.Customer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="20">
        <!-- Create New Customer Button -->
        <Button Content="Create New Customer" Click="OnCreateCustomerClicked" HorizontalAlignment="Left" Margin="0,0,0,10"/>

        <!-- Customers Table -->
        <ListView ItemsSource="{x:Bind ViewModel.Customers, Mode=OneWay}">
            <ListView.Header>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Name" Width="150" FontWeight="Bold"/>
                    <TextBlock Text="Phone" Width="150" FontWeight="Bold"/>
                    <TextBlock Text="Email" Width="200" FontWeight="Bold"/>
                    <TextBlock Text="Points" Width="80" FontWeight="Bold"/>
                    <TextBlock Text="Actions" Width="250" FontWeight="Bold"/>
                </StackPanel>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Customer">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind Name}" Width="150"/>
                        <TextBlock Text="{x:Bind Phone}" Width="150"/>
                        <TextBlock Text="{x:Bind Email}" Width="200"/>
                        <TextBlock Text="{x:Bind Point}" Width="80"/>

                        <StackPanel Orientation="Horizontal" Width="250">
                            <Button Content="Detail" Click="OnDetailClicked" Tag="{x:Bind}" Margin="5,0"/>
                            <Button Content="Edit" Click="OnEditClicked" Tag="{x:Bind}" Margin="5,0"/>
                            <Button Content="Delete" Click="OnDeleteClicked" Tag="{x:Bind}" Margin="5,0"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
