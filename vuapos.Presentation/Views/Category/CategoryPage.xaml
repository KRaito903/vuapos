<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="vuapos.Presentation.Views.Category.CategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:vuapos.Presentation.Views.Category"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="White" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Content="Add Category" Width="150" Height="40" 
                    Click="AddCategory_Click" Background="#C19256" Foreground="White" 
                    BorderBrush="Transparent" Grid.Column="0"/>
            <AutoSuggestBox x:Name="SearchTextBox" QuerySubmitted="CategorySearchBox_QuerySubmitted"
                            TextChanged="CategorySearchBox_TextChanged" Width="300" Margin="0,0,10,0" 
                            PlaceholderText="Search Category..." VerticalAlignment="Center" Grid.Column="4"/>
        </Grid>

        <!--<ContentDialog x:Name="AddCategoryDialog" Title="Add New Category" 
                       PrimaryButtonText="Add" SecondaryButtonText="Cancel" Closed="AddCategoryDialog_Closed">
            <StackPanel>
                <TextBlock Text="Category Name:" />
                <TextBox x:Name="CategoryNameTextBox" />
            </StackPanel>
        </ContentDialog>-->

        <ListView x:Name="CategoryListView" Grid.Row="2" Margin="0,0,0,20" 
                  ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}"
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ListView.Header>
                <Grid Margin="10" VerticalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Name" FontWeight="Bold" VerticalAlignment="Center" 
                               Margin="10,0,10,0" Grid.Column="0"/>
                    <TextBlock Text="Action" FontWeight="Bold" Margin="0,0,100,0" VerticalAlignment="Center" 
                               Grid.Column="1"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Category">
                    <StackPanel>
                        <Grid Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" FontSize="16" 
                                       HorizontalAlignment="Left" Margin="0,0,10,0" Grid.Column="0"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="1" VerticalAlignment="Center">
                                <Button Background="#C19256" Content="Edit" Width="75" Height="30" Margin="5,0,0,0" Click="EditCategory_Click" DataContext="{Binding}"/>
                                <Button Background="#C19256" Content="Delete" Width="75" Height="30" Margin="5,0,0,0" Click="DeleteCategory_Click" DataContext="{Binding}"/>
                            </StackPanel>
                        </Grid>
                        <!--"<Border BorderBrush="Gray" BorderThickness="0,1,0,0" Margin="0,10,0,10"/>-->
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>